{
  "$schema": "https://deno.land/x/denon@2.4.8/schema.json",
  "scripts": {
    "test": {
      "cmd": "deno test index.ts",
      "desc": "Test Slurp-server"
    },
    "start": {
      "cmd": "deno run index.ts",
      "desc": "Start Slurp-server"
    },
    "compile": {
      "cmd": "deno compile --target=x86_64-unknown-linux-gnu index.ts",
      "desc": "Compile Slurp-server"
    }
  },
  "allow": {
    "env": "SLURP_SERVER_MYSQL_HOSTNAME,SLURP_SERVER_MYSQL_USERNAME,SLURP_SERVER_MYSQL_PASSWORD,SLURP_SERVER_MYSQL_PORT,SLURP_SERVER_MYSQL_DATABASE,DENO_DEPLOYMENT_ID",
    "net": "db-mysql-ams3-57019-do-user-8895515-0.b.db.ondigitalocean.com:25060,0.0.0.0:8080",
    "read": ".env"
  },
  "logger": {
    "quiet": true,
    "debug": true
  }
}
